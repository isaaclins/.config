- [2026-07-15] Isaac's delegation policy: when delegating to subagents, always track/report usage cost, write tightly scoped self-contained prompts, and prefer cheaper models (e.g. haiku/sonnet-class) unless the task truly needs a top model.
- [2026-07-15] Before reverse-engineering any tool's behavior (grepping dist bundles, reading minified source), FETCH its online docs first (web_search/fetch_content). Isaac explicitly prefers docs over source archaeology.
- [2026-07-15] Memory has two scopes: `remember` tool with scope="global" (or /remember -g) writes user-wide notes to ~/.config/pi/memory.md; default scope="project" writes repo notes to .pi/memory.local.md. File user preferences/habits globally, repo facts locally.
- [2026-07-15] When delegating interactive/implementation work while running inside tmux, prefer spawn_agent (visible tmux pane, user can watch and correct, steer via agent_pane) over the headless subagent tool; use headless subagent only for parallel batch/background jobs where visibility does not matter.
- [2026-07-15] NEVER spawn helper agents on fable/opus-class models or high thinking. Local pi-codrive config uses openai-codex/gpt-5.6-luna at thinking low; keep delegation cheap and check subscription usage (/usage) before expensive work.
- [2026-07-16] Isaac wants subagent delegation done via spawn_agent (visible tmux pane / live pi session) so he can watch and interact, not hidden background subagent runs.
- [2026-07-16] spawn_agent (pi ext ~/.config/pi/extensions/spawn-agent.ts): in tmux, each spawned subagent reports agent_end directly over private authenticated PI_CODRIVE socket IPC. The parent is auto-woken with the full capped report inline; agent_report is optional history and agent_pane is only for live inspection or steering. One wake-up per child turn. Outside tmux, Ghostty fallback remains manual and user-driven.
- [2026-07-16] spawn_agent is strictly one level deep: socket-authenticated spawned children get an error from spawn_agent/agent_pane. Never delegate sudo or interactive-password tasks to agents; ask the user to run them in a real terminal.
- [2026-07-16] After verifying a spawned agent's work is complete and correct, kill its tmux pane (tmux kill-pane -t %N); keep it open only while its context is still useful for follow-ups or fixes.
- [2026-07-16] When testing pi interactively in a sandbox HOME, start from a neutral cwd: a cwd with project resources triggers a modal trust dialog that silently swallows send-keys input and looks like a hang.
- [2026-07-16] Isaac's context-hygiene workflow: when an agent decides context should be summarized or cleared, it must first write an inline handover document in the conversation (not on disk) capturing the task state and next steps, then clear the unnecessary context and continue working; /context prints inline precisely so the model can see its own usage and trigger this.
- [2026-07-16] Homeserver: `ssh isaaclins@homeserver` (fish abbr sshh); runs Ubuntu in UTC, user systemd with linger enabled; claude-session-anchor.timer (user unit) pings claude haiku at 04/09/14/19 Europe/Zurich to anchor Claude Max 5h usage windows; CLAUDE_CONFIG_DIR there is ~/.config/agents/claude and must be set for non-interactive claude.
- [2026-07-17] Pi's TUI emits Kitty graphics APC sequences (e.g. "Ga=d,d=A,q=2") that tmux interprets as pane-title writes; block with per-pane `allow-set-title off` (tmux >=3.4), which still permits `select-pane -T`.
- [2026-07-17] Never spawn the 'luna' model for programming/agent tasks; use opus or sonnet instead (user was frustrated by luna being used).
- [2026-07-17] Promoted to orchestrator role for isaaclins: whenever a task touches the homeserver, delegate it to the standing Pi agent already running there (tmux session "pi-standing", 24/7) instead of doing the work directly via SSH. Reach it via the `sshpi` fish function, or programmatically via tmux over ssh.
- [2026-07-17] To delegate a task programmatically to the homeserver's pi-standing tmux session: ssh in, `tmux load-buffer` the task text to a file then `tmux paste-buffer -p -t pi-standing` (the -p bracketed-paste flag is required — without it, Pi's TUI treats embedded newlines as submit and truncates multi-paragraph messages after the first paragraph), then `tmux send-keys -t pi-standing Enter`. Read replies with `tmux capture-pane -t pi-standing -p`.
- [2026-07-17] homeserver (isaaclins@homeserver) environment notes: default login shell is fish, not bash — ~/.bashrc PATH edits don't apply in interactive fish sessions. Pi is installed there via npm at the absolute path ~/.npm-global/bin/pi (always use the absolute path when scripting). Pi config (extensions incl. compaction-resume fix, lib helpers, models.json, settings.json, AGENTS.md, skills, auth.json) was mirrored from the laptop into ~/.pi/agent (symlinked into ~/.config/pi and ~/.config/agents, same layout as the laptop), adapted for headless Linux (dropped macOS-only externalEditor/shellCommandPrefix settings and the pi-computer-use package).
- [2026-07-18] When using spawn_agent, don't poll with Bash sleep; completion reports arrive automatically. To check in on progress, use agent_pane (read) or agent_report instead of sleeping.
- [2026-07-18] User prefers Claude Opus 4.8 over 4.6 for delegated/agent work when available (model id: anthropic/claude-opus-4-8).
- [2026-07-19] Isaac has flagged repeatedly that I leave verified spawn_agent panes open instead of killing them. Treat pane cleanup as part of the completion of any delegated task, not a separate followup: right after accepting a subagent's work (or after superseding it with direct fixes), immediately tmux kill-pane it in the same turn, don't defer it.
